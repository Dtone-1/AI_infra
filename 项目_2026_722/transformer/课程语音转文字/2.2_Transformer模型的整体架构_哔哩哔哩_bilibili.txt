那么 接下来我们来讲解一下Transformable模型的一个整体架构如果你想把这个Transformable模型需要了情况下你必须要对整体的架构有一定了解同时在这个架构的基础上面结合其中的一些基本小模块和其中的一些代码进行学习最后将这样的一个基本小模块组合成这样一个整体网络这样的话你就会对这样一个Transformable模型的整体架构有一定详细了了解那么我们来看看这个网络模型的整体架构是如何的首先这里这张图是来自于Transformable模型这样的一个论文当中的一张图我低眼看到这张图的时候我也觉得这样的一个网络模型是很优美的很漂亮但是仔细一看我发现这样的一个网络模型其实它还是头脱不了我们一些经验网络模型的一些特性比如说你会发现的其中那些模画还是复用的比较多对不对前面我们所讲的一些ResNet也好危机机也好其实它后面都会有个干什么18啊干16等一些数字这些数字可能代表其中那些网络的层但其实你仔细分析你也会发现的这些数字也和其中那些网络模型层的复用有一点关系也就是这些网络都是有一些基本模块不但的重复去跌带对跌构建的那么其实我们看Swim模型也是逃离不了然后我们来首先看一下我们前面也讲过就是Swim模型它而由编码器和解码器构成的很显然这里成个N对不对说明这个编码器和解码器里面的一些我的模型层可能可以不断去跌带比如说这里是10的话可能里面的一些网络层就跌带10层就把这个模块复用10次嘛当然我们后面去在讲代码的时候会详细去讲不用去担心然后我们先从哪里开始讲从这里的输入开始讲首先我们的输入是什么Swim模型的一个论文当中做的是记忆翻译我们就用记忆翻译来进行这样的一个分析这里的输入假扯我们是英文翻译成中文的情况肯定是英文吧当然我们知道就是计算机它是识别不了这样的一个文字的它是需要把这个文字转换成什么呢转换成一些编码或是转换成数字我们简单说就转换成数字它才能识别计算所以可能在我过程当中要做一些就是把我们的自然语言就是我们的文字转换成数字这样的一个过程或者说转换成这样的一个数字相量这样的过程所以这里输入的话是将我们的文字转换成词签入相量由自然语言转换成这样的相量千不用去管只知道它是一个相量矩正就行了后面我们这一块会详细去讲然后的话我们前面也讲了这些自然语言的文字它是有时间序列顺序的所以这里加一个位置编码来代表它的一个时间序顺序这个位置编码它也是一个相量也是一个相量最后这里做个相加的操作把这个相量两把相量一融合就是这里的一个词相量和这里的位置编码相量一融合所以这里的输入就带有我们这里词的信息或者说翻译句子的信息和位置信息所以这里是个位置编码提供什么时间线量序列的信息的然后这里就是多偷注意机制了对不对是一个视频网络层后面我们会详细去讲然后这里在做个什么A的和A的就是什么惨惨连接这个操作然后这里做一个惨规划这个操作这也都不是很难在这个操作我相信你学到它是科贸这个模型的时候你还是有一定这个前面网络模型基础的否则没法学嘛 对不对然后的话这里在做一个前卫士尼网络对不对前卫士尼网络前卫士尼网络这个你就可以理解为这里的时候你就可以理解为全连接是尼网络也行好不好然后的话当然还是有点不一样的然后的话这里在做一个惨惨连接和惨规划所以这里的输入如果你学过前面的课程你就知道这里的输出就是什么从这里和这里的提取的一些信息对不对然后这个信息就传入到解码系当中当你会发言的这里我们就认为是信息吧或者说有用信息对不对网络会进行提取的然后呢当你回来这里好像还有一个输入这输入是什么那我们来稍微讲一下首先假设这里我们输出的英文I love you对不对这里是我们的输出从理论者来说I love you这三个词输出应该是什么我爱你对不对你送到模型当中这三个单词肯定是什么同时输入进去的那么事实上最后模型的输出并不是同时输出来了而是有个先后顺序的比如I love you输入进去之后那这里呢他经过期许之后呢这里呢就给一个期时间或者说给一个开时间告诉他假设这里开了一件假装S对不对S这个符号加一个豆腐吧S符号他输进来之后呢经过期许中间的一些什么神经网络层啊不然不然不然去期许然后输出结果呢是我而不是我爱你三个词然后的话他中间有个这样那个循环的这样的一个过程再把这个我放到哪里放到这里这个时候呢他输入的是这个然后呢再经过这些神经网络层啊不然去期许不然去期许然后呢再输出爱放到这里爱然后呢再把什么呢把这个三个输到网络当中然后呢不然去期许结果输出你然后呢然后呢这个你再放在这里然后呢这几个呢再输到这里面去然后呢不然去经过这个网络层去去去输出输出然后做输出然后有个结束比如说intern然后呢这里输出一个intern然后出现intern的时候呢说明这个模型呢就结束了就不用再做这样一个循环了最后呢输出结束呢就是我爱你所以你会发现呢在编码系里面呢他的输入啊是一股脑输入的无LV对于一起输入的但在最后模型的那个输出的时候呢还是根据前面的一个输出最后呢不然去跌在把后面的那种输出的那或许啊然后这里你不知道老实说他都说什么东西啊对不对那么我们来体验一下GPG大家应该用过或者说大模型大家应该用过对不对我们来感受一下比如说我给大模型一个请帮我说明比一下CNN网络对不对你看看他这里面的输出都是基于前面的嘛还有一个字一个字输出的当然呢因为现在大模型呢比较快所以呢你感觉他是很快就输出啊其实让他是基于前面对不对他是基于前面所以的话基于翻译也是一样他都是基于前面的内容啊进行这样这个最后的内容都输出的对不对前面的这个输出来对我才输出这个然后呢当然他可能的一个词一个词或者一个句子输出的明白我的意思吧大模型他是基于传播模型架构当然呢他不是基于简单的传播模型的他中间做了很多优化但通过刚刚的演示你也会发现他的输出就是基于前面时间的一些文字的输出在输入下十个的输出的对不对随当的模型架构大概应该清楚的然后的话所以这里的输入这个相量啊也就是我们刚刚说讲的也是一些歧视服啊结束服啊包括一些中文的一些词相量对不对当然这里也有实际有位置信息的因为我们所有的输入的一个实际学历的信息啊都有位置信息的然后的话这里一看他也是多多注意机制啊和这里本质上一样只是这里带了一些什么英国烟马而已这后面我们回去想这个英国烟马很重要很重要也是我们整个模型当中一个比较经验的一个知识点也是个难点然后到这里也是一个多多注意机制但是我颜色画得不一样为什么因为这里的输入啊很来自于我们这里的编码器你看看还来自我们这里的编码器这里的输然后就不用说了这里的全程阶层那就是很简单很简单对不对大概清楚了最后呢这里Softmax输入对的词的或者对的句子的这样的一个相量比如说我它可能在次表当中是一个位置它的结果盖律值的零先纠那么最后输的结果呢就是我对不对这其的本质上到这里就是一个分类任务了嘛对吧随到这里如果你对这个模型不了解的情况下应该对这个模型经过我的讲解之后应该会有个大的了解了其中的一些模块呢很显然我们只需要对其中的一些基本模画结合的代码详细去讲解最后把还一组合到一起去整个Transformer模型的架构你还能学不会吗那我是不相信的对不对现在来到我们来从这里的输入看著一步一步的曲响