那么接下来我们来讲讲一下关于Transformer算法模型的一个算法背景Transformer这个算法模型在我们整个深入学习领域当中是极其重要的一个算法它的发明是一个开创型的一个发明因为在它被发明之后后续的很多深入学习的相关工作都是记忆这个算法模型或是记忆这个架构去做的所以这个算法对我们生活当中的一些人工智能应用产生了极大的影响那么这个算法是有谷歌这个团队研究起初的早在2011年就被发表了也就是被已经被做成了它主要工作是干嘛的在时间虚烈建模当中做的一个任务当时任务当中它是在记忆翻译当中做的一些时间虚烈当中的一些时间验证那么时间虚烈任务它有一个很大的特点是什么量它是有顺序的或者说是有时间顺序的那么自然语言这个方向呢现在基本上都是由这个Transformer这个架构去做那自然语言这个方向呢它有个很典型的这个特征就是它是有时间顺序的很简单我说一句话这句话就是自然语言它是有顺序的对不对你把这个话你打乱的或者说是去排错了它的一个话的这样的一个这样的一个意思就很难去让人去理解了对不对所以呢这样的一个算法模型一开始主要是在这个虚烈建模当中去做一些验证当然我们后来知道这个算法模型摆应用的各行各个领域当中包括视觉包括挖掠数据挖掠等一些领域当中对不对然后它主要提升了什么来模型的一个并行性我们来看它这个并行性这个是针对哪个算法的呢是针对我们循环性的网络当中的一个算法的早期的一个循环性的网络比如说安安算法比如说STM算法它这些模型它一个很重要的一个特点就是并不能并行去做数据处理为什么呢你看说原来要循环性的网络它而对我们数据处理或者对于模型的数据的输入做循环的所以在我们时间序的战略领域当中它会把第一个时间的数据租到模型当中模型进行计算之后再输入模型的数据输出结果然后再进行下一次个比如第二时刻的模型的数据输入再得通的最后的结果然后再进行第三个时刻的模型的输入再得通的结果所以这样那个计算过程是极其低下的所以在我们传说模型当中它的模型的输入数据是可以并行的这个效率就大了几个了而第二个长距离的依赖键模能力那么刚刚我们也讲了我们的数据它是有带有时间序列的也就是数据这第一个时刻的数据X1这第二个时刻的X2这第三个时刻X3遇到DN个时刻的Xn对不对这是有讯区的那么早期的在我们时间序列战或者说在我们这样的战员方向当中我们用的算法是NXT们的机器变成了一些算法这个算法有一个还有一个很重要的特点就是什么呢就是我可能能学习到这个时间段的这个信息但是可能对前面一段时间的一些序列的一些信息做了一些疑问导致可能这个X1X2可能对我们做过的输出结果也很重要对不对那么这样装来信息被遗忘之后最后对我们模型输出结果会产生一个很大的影响事实上这里的S&M对这个安娜进行了一个改进它也是致力于或者说环境于这个场具力依赖这样的一个问题的包括一些其他变种算法都是可能想解决这个问题的但是都没有很好的具体解决但是它是方法这个算法它基于什么注意力其实是个算法去解决这个问题或者说去环境这个问题应该是比较严谨的所以川斯风的这个算法它这样那个诺文那叫做Attention is all you need这个基本上在我们整个深入学习领域已经成了一个根了对不对这个这句话呢那可能被我们言行为是吗Uro is all you need的对不对比如说CN is all you need比如说你发明的一个什么算法什么 is all you need对不对所以整个这样的一个川斯风的算法模型在我们整个深入学习领域影响是深远的包括在一些弹性领域当中就是大家可能拿这个诺文的一个期望再做调侃对不对然后我们来看看这个川斯风的这个模型它主要做了哪些工作第一个在结构上面它用编码器和接码器的这个结构这个结构并不是传斯风的这个算法模型的替快的它也用了别人的一个这样一个早期前任做了一些工作的一个结构然后它用了三种注意力机制事实上我们来看看编码多头注意力机制交叉注意力机制和接码多头注意力机制先讲一下这三个注意力机制都是一样的只是以为有一些细微的小差别而已所以不要被这个三种注意力机制下早基本上它们都是多头注意力机制后面我们再讲模型结构的时候会详细去讲然后它还会在什么我们输入数据当中做了这样的一个什么位置编码来帮助我们数据数据这样的一个时间训练这样的一个信息然后呢这里面的结构呢还采用了惨差连接惨规一画惨差连接大家都知道我们是来自于Resnet的给我了模型是吧还有惨规一画前会是你网络这都是比较激持的网络形成了这样的一套标准的模型跨然后呢可以使我们这样的传斯风模型架构可以不断的就是跌层比如说我可以有多多层的这样的一个编码器或者减码器的结构因为我这里有惨差模型结构是不是就可以使模型更加稳定的生存训练整个结构就是这样子那么其实我们前面也想了整个喘斯风模的这个算好模型对我们整个未来那一些模型结构有很深浅的影响比如说在我们这样的一个NRP里面有自然语言那个零当中比如说我们把它这个模型包括一些大模型系列那这个就是几乎各位同学看我的视频的同学应该每天逃离不了这样每一个应用了比如说最经验的恰的GPT对不对那这是用我们OpenAI这个公司去做的这个真的是怎么说从他发明之后整个这样一个大模型这样一个领域挂起了一阵房几乎大公司小公司都在做对不对包括这件工作当中的一些公司你说规模也不是很大小公司也要做当然他们做的肯定是不是说开发这样的一个大模型而是把这些大模型应用在各行领就是这个各行各业党做一个就是扩展应用包括这里的我们国内的这样的一个DBC这样的一个应用对不对这个在今年或者说在这个最近一段时间还是比较好对不对还有一些就是谷歌这个公司开发的这样的一个就是Dina这个大模型包括阿里这样的一个千问这样的一个大模型都被开发出来了当然在大模型那个系列当中不仅仅仅是四个国内还是很很多公司能有实力去做的比如说一些斗包这样的一个APP对不对还有一些强的一些大模型都是基于这个Transformer这个上网模型去做的所以呢足以说明这个上网模型对我们这个生活当中对我们未来的人的一个出行生活当中这个影响然后还有一个就是我们前面讲了Transformer这个模型它其实不仅仅于就是在我们自然圆这样的一个方向当中有影响它做的当时也提到在论文当中也提到这个上网模型可以扩展到别的一些领域当中比如说图像当中比如说后续一些做的在Transformer这个架构当中发明了一个这样的一个基于市民的这样的一个Transformer模型VIT这个上网模型做一些就是图像分类等一些工作包括一些其他都模太大模型就是大模型不仅可以输入文字还可以输入图像输入一些其他文件进行一些分析所以到这里大家应该生活当中应该切实的赶回的或者体会到这些Transformer模型对我们整个人这样的一个生活的一个影响所以足以说明这个上网模型这样的一个重要性那我相信如果你是在读言或者在读本科你的论文当中如果用到一些相关的上网模型可能Transformer模型大概是跑不掉的那么接下来我们来详细讲解一下关于Transformer算了模型的一些主要内容