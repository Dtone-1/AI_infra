前面我们已经很详细地讲解了如何去构建辞欠入相当举政去表达自然语言数据那么接下来我们来看看这个模型的整体结构图从这个模型整体结构图你会发现的我们到现在才讲了下面这个小部分之间那么我们这里来看一看模型的数是一个自然语言的数据这个自然语言数据需要转化成辞欠入相量就是这个部分而辞欠入相当需要和这里的位置编码做一个融合最后才能输入到模型当中为什么这么去做呢首先我们在做一些时间顺利的任务当中比如说翻译任务输入的数据是一些英文句子或者一些其他语言一些句子而句子的话它是有很明显的先后的这样的一个顺序的所以的话我们输入的数据它只带有这样的语义的一个相当性期而不再有先后顺序的这样的一个信息所以这里要够进这样的一个位置编码而位置编码里面就表达了每一个这样的一个辞相量对于这样的一个先后顺序这样的一个信息最后再一做融合之后或许这样的一个相量是带有我们对于的语义信息和对于的相量先后顺序这样的一个信息最后才可以做模型的一个训练、推力和验证能力也相过来跟着所以这里这样的一个位置编码这样的一个指点在我们整个模型当中是一个很重要的指点那么加来我们来看看位置编码这样的一个指点是如何去构建的那么在Transformer这样的一个原理当中这里的位置编码是用这里的一个Sy和Cosy的三角韩数去构建的那么我们来看看怎么去构建的首先这里我想讲一下在生动学习当中大家不要去害怕看无视在生动学习当中很多公式其实相当于说比较简单的可能你只需要有一些出动和高弄的一些指摄就可以搞懂所以不要害怕那么我们来看看这一的批译表达的就是位置编码这样的一个数值相量数值那么这个批译是有什么所构建的是由这里的这样的一个Sy和Cosy所构建的那么Sy和Cosy的韩数你们来数值是由哪些参数去控制的很明显这里有哪些数值PSOPOS和i还有地仗的一个数值去决定的对不对那这里我们看看POS是指什么来当前偷看在训练当中的第几个位置对不对 第几个位置那么这个就很容易去理解了打给难民方了挨捞U这样的一句话做积极翻译的时候还没显 挨还在第0个位置捞的是在第1个位置U是在第2个位置所以比如说我们来计算捞这样的一个位置编码的时候这一道POS应该是等于1的对不对然后我们先看一下地地这个参数是什么前面我们在讲辞签路相当的时候我们又提到过参数一个辞签路相当的围堵大扬应该是非常低当的大小也就是也就是V是我们的一个整个辞表大小对不对说白的就有多少个偷看这样的一个辞卖低纳是代表每个偷看这样的相当的围堵大小比如说从中间取一个偷看对象量比如这个这个相当的明显就是1成1地在我们transformer当中这个地确的多大512所以很简单假如捞这个词它的词相量应该是这样的相当的数值比如说里面数值0.530.260.170.38那么一共有多少个有这个相当大小时51成1地第二等512也就是有512个这样的数值所以的话如果想把我们这个词相量信息输到模型当中它还要配个这样对应的这样的一个围着编码信息围着编码信息那这里大家应该清楚了围着编码这样的一个相当大小应该也是1成1地涨的大小对不对应该也是1成1地涨的大小所以才可以做相加嘛那么围着编码这里是有两个这样的一个三点还是说够建成的所以爱呢可能是代表对应的这样的一个相量比如说可能第1个围着相量第2个围着相量和第3个围着相量对应的这样的一个位置去计算对应位置这样的一个相量编码的数值比如说我们来算一下那以这个以这个公式为例稍微解一个图吧那么我们可能第1个围着第1个围着呢那可能我们利用了就是这样的一个公式去做计算那么赛呢里面上面的数值应该是等于1嘛因为这个捞这个在我们这个巨大中啊它的位置是1对不对然后下面有个1万对不对然后第1512那这里呢位置是多多的应该是0因为我们从0开始嘛对不对从0开始对不对那么很明显就是2×1.0这样的一个围着相量还是是能算出来一个数值的对不对飞到旁啊这里假特有个数值假特就到0.2吧打个比方啊第2个呢这样的一个围着呢应该按到底它是什么这是第0个围着第1个围着第1个围着呢它是一个技术围对不对所以我们利用的是扩塞去做计算扩塞去做计算扩塞去做计算那扩塞呢也可以把它拿过来对的是这个相量的位置边码那是怎么算呢扩塞下面应该一万分之512这个指数512二成一一对不对然后呢这里上面是一这个扩塞的一个指也是能算了假如是0.3那下面这个呢再用塞去做计算然后呢这里再用扩塞去做计算这里再用塞去做计算不但去铁带直到512个数值全部计算了对不对然后呢这里面呢这个每个这个相量的PoS是固定的第十固定的那就通过这个对的相量的位置对的相量的数值的位置然后那个爱去做计算就ok了这样的话对的这个此相量啊它都会有一个对的这样的一个位置边码同理呢这样的这个U和后面那些其他句子都可以对的相量边码啊所以在这个相量边码呢事实上它是固定的嘛每个位置的相量边码还是固定的嘛所以的话你看第零个位置的相量边码应该是这个一行的所有的数值对不对这里我只展示了六十个维度的一个数值啊按照理这应该是五百一十二个数值对不对第二个呢应该是这么多这里同理呢第三个应该是这里啊一直往下走一直往下走对不对这是第五个位置的这样的一个这边码信息所以呢这个位置边码这个轰扎并不是很难整个利益进来也不是很难那么我们来看看啊加上我们这里的一个例子我是一条狗一共有零一二三四个位置对不对假设我们这里这样的一个相量啊它的第一维度是等于三的所以这个时候呢第零个这样的一个相量信息啊它的位置边码应该是零一零因为这里的PoS是等于零的嘛你去算一下了这样的一个塞和扩塞它的数值是不是零一零啊第二个呢是这样的一个自然语言这样的一个相量啊它的一个词相相量对不对这边码呢应该是这些数值第三个呢同理也是一样第四个也是一样啊都可以通过这里的塞和扩塞去计算出来那么这个时候你快把呀我这个词相量这就是唯独低嘛是吧然后呢就可以和对应的位置边码一相加得到这样的个数值啊这个数值呢包含着我们磁和位置边码对应的信息同理这里也是一样所以这里啊说的是一个句子这个句子呢包含着对应的自然语言的信息和位置边码的信息或者说时间序列信息这样输到我们的Transform模型当中它就可以有效的学习到它的语言信息和对应的这样的前后顺序关系的信息啊所以这里就是位置边码这样的一个知识点并不是很难那么事实上到这里啊模型的输入部分呢才讲完而这样呢我们可能要去看一看对应的模型的确定的一个内部结构的