那么前面我们已经将这里的一个位置编码的相关支点已经讲解完了那么加了我们来看看Transformer模型的整体架构那么Transformer整体架构它包含这里的编码器和节码器的部分而编码器和节码器的部分如果你仔细去分析的情况下你会发现其中一些网络模型层的一些结构都大塌不塌的那么我们知道Transformer的论文它的一个题目叫做Tation is all unit的所以足以说明Tation将你一个算法模型在我们整个Transformer当中的一个重要程度那么事实上你会发现这里用的是一个多头注意力机制这里用的也是一个多头注意力机制这里用的也是一个多头注意力机制那么在我们编码器当中这里的多头注意力机用的是自注意力那么事实上在我们节码器当中这个多头注意力也用的是自注意力当然这里多头注意力机制还用的不是自注意力但在本质上它和我们这里的自注意力机制去别大塌不塌只不过它的一个输入的一个部分来自于不同的网络层而已整体的结构是一样的所以从土豆年也会发现这里的输入好像来自于别的层而这两个模型的一个输入好像都是来自于前一层的一个输入输入对不对所以这个三个这样的一个注意力机制本质上差别不断它的一个底子都是来自于这里的自注意力机制所以我们只需要把这个理的一个自注意力机制搞通那么事实上关于我们整个Transformer模型架构就我估计它已经搞定了60%不夸张加上这里这样的一个位置编码和这里的输入这样的一个辞欠入相当举振那么事实上就已经搞定了20%那可能还有一些20%的支持点内容呢可能再一些其他网络层的所以将来我们来看一下这里的自注意力机制这三个模型结构是如何去实现的