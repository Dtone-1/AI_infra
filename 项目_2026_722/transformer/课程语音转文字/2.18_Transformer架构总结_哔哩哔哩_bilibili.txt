那么前面我们已经将交叉助理机制讲解完了那么到这里关于我们整个 Transform模型架构当中的所有基础文化就已经讲解完了那么我们花了很大经历在这个三个类型的助理机制上面去讲解那么我们也知道 Transform模型的一个Lugna叫做Attaction is all unit的那足以说明这个助理机制的一个重要程度所以还是那句话你必须要把这个几个助理机制理解清楚你才能对整个架构有个很清晰的了解接下来我们来基于所有的基本的小模块然后将模型的架构构建出来看一下模型是怎么样的一个整体兵型的那么我们这里有这样的一个模型假设我们现在做的是一个中文翻译成英文这样的一个任务那么我们输入的一个数据应该说是这里的一个假设我们是我是一条狗这样的一个中文对不对那么这样一个中文自然远会将那个数据转化成那个对应的此切容相量和这一的位置编码相融合任何完之后作为我们编码器的一个输入编码器的输入这个时候这里包含着我们时间讯的讯息也包含着我们自然远远的讯息然后呢作为我们第一个模块做的这里的自主理机制或者说呢多头主理机制这样的一个输入输入呢我们要签计算出来QKV吗对不对QKV呢作为自然远作为我们这里的自主理机制的一个输入来计算对应的这样的一个自主理机制的一个得分取证然后呢我们从这个原始信息当中啊或者说从这里的输入信息当中啊提取对应的这样的一个信息那么提及完这样的一个信息之后呢我们这里还有一个成归化这个成归化是对我们这个信息啊作战的一个归化对不对归化完之后呢我们从这个原始的这个输入这里还有一个产差连接产差连接然后呢再加那个信息啊再做进一步融合进一步融合然后呢到这里这里的话啊我们经过这样的一个前贵说您网络啊对我们原始输入信息啊再经过这样的一个前贵输入然后呢哇这里还有一个跳展连接这样的一个方式把我们的信息再融融合同理呢这个前贵成就网络的输出啊还是要做这样的一个成归化的那么到这里呢一个最基本的编码器模块就已经够进去了当然我们知道这里成了一个N对不对在我们那么当中这里的N是应该是等于6的也就是对我们这样的一个模块复用了64是吧比如说这个输出作为下一个编码器的一个输入然后呢不但这样叠带64之后呢最终的一个输出可能是要作为我们这样的一个结码器的输入的啊这里要清楚啊那么我们知道如果我们在做这样推力过程当中啊我们第一步的情况下这里应该输入的是一个开始间对不对开始间告诉我们这个结码器开始工作了那么开始间也是有并的自然原信息的所以呢我们也需要这段转换为什么对于这个此切容相量和对应的位置编码相容和然后作为这里的英国研码多头助理机制的一个输入那计算出来定的KVQ然后呢计算对于的助理机制得分取证然后输出来结果呢依然是要做这里的成归化和这样的一个跳软连接的一个这样的一个操作的那么这个时候呢这个模型的一个输出啊就包含着我们原始的这样的一个就是开始间的一些信息对不对那么这样的信息呢是作为我们这里的一个交叉助理多头助理机制的一个输入什么计算对应的Q我们刚刚已经讲过了然后前面这个编码器的一个输入呢要计算对应的K和V然后同理呢这时候又计算对应的这样的一个助理机制得分取证然后呢从我们这样那个编码器的信息当中啊提取对应的这样的一个信息对不对那信息是在哪里在这里当然呢这里呢还和我们原始的这样的一个信息啊再做进步上的融合所以呢到这里啊到这里的输出啊它继包含着我们编码器里面的信息也包含着我们结码器的信息这个时候呢再做一个前盔神经网络作战的一个硬设然后呢再做这个跳转连接这样的一个方式作战的一个连接同理呢还会有这样的一个成归化最后呢在经过一个限性全联接层经过的Softmax含书的一个硬设我们能得到下一步的翻译的单词的一个概率假设我们这里翻译了句子是哎对不对这时候呢再把这个输到我们这个模型当中和刚刚一模一样去做计算我们那个编码器这个时候呢这一部分呢就不需要再重复做计算了对不对然后呢不断的输出假设是那么把这个M调到这里来对不对再把这个输到这里面来然后呢可能是输的是额再把额呢在调到这里来直到我们最后输出的是一个结束这是我们整个推理的过程当然训练过程也是一样啊训练过程呢无非是将我们的标签是不是I am a dog输到我们这个模型当中和这个我们那个汉语也输到模型当中并行去做计算然后基于每个单词他都会有最终的深层的Loss值然后呢通过这个Loss值对我们这个其中的参数参数做最后的一个反向传播对参数进行更新嘛这个就是我们整个Transform模型的架构啊我希望那样啊拿到这个模型结构土的时候也可以和我这里一样能把整个这样的流程啊去理解清楚这样嘛你至少对整个这样Transform模型的架构应该是有个最有个很轻轻的了解了这就是我们整个Transform模型的架构那么事实上呢我们刚刚也讲了在我们Transform模型当中啊它这个incord的这样的一个层啊和我们的dc的这个层呢也就是编骂系层和接骂系层啊我们刚讲的它成了一个N在论文当中呢它这个N是等6的对不对也就是我们分了6次所以呢你看这里有个输入经过我们第一层编骂系层第二层编骂系层第三层编骂系层一到第六层编骂系层它这里的输出才作为我们那个结骂系的输入了看也没有但做我们结骂系输入呢它是作为我们每一层结骂系的输入的每一层结骂系的输入的然后呢和其中呢交叉入理智着做计算做计算最后呢这里的输出呢是对呢每个Token的这样的一个概率值当然呢其实还有别的一些方式嘛比如说最后一层的一个输出作为每一层的就是结骂系的输入那事实上呢其中的一些别的结骂编骂系层是不是也可以做一些作为一些其他的一个结骂系层的一个输入呢依然可以那方式有多种比如这种比如这种比如这种比如这种当然在论文当中它什么方式呢是这种方式是这种方式随到这里啊我们整个它说模型的一个整体的一个架构和流程呢我们就已经讲解完了那么意味着我们整个原理部分呢基本上就已经结束了那这样呢我们会讲解实战内容结合我们的代码再把我们这个这样的一个模型架构进行个分析让你对这个模型呢有个更深刻的了解