1. moe模型结构中共享专家和路由专家通常是并行还是串行，还是说不同模型之间不一样
2. 在该仓库的代码中，welm的moe模型结构中共享专家和路由专家通常是并行还是串行，具体的实现在哪些文件
3. 整个仓库就是在适配welmv4.5模型的结构，请查找moe结构中共享专家和路由专家通常是并行还是串行相关的文件
4. 主要分析`dabieshan/welm`分支

- showme以及arctecture

1. 请根据当前仓库的结构找一下sglang中有其他模型moe共享专家和路由专家并行的方案吗，找出最适合该仓库welm模型的方案，告诉我具体的位置
2. 我应该修改该仓库的哪些文件来实现moe共享专家和路由专家并行，具体修改哪些地方
3. 现在我的welm模型是4卡，所以说要实现moe结构中共享专家和路由专家并行，需要将共享专家复制到四卡，路由专家被平均分配到四卡是这样吗，我的仓库目前是不是还没有实现两者并行，我要完成的任务如下：WeLM 模型在 SGLang-MLU 的 MoE 层中同时包含 Routed Experts 和 Shared Expert。当前两条计算分支在同一个 MLU Stream 上顺序执行，导致 Shared Expert 与 Routed Experts 之间没有计算重叠。目标是将 Shared Expert 放到独立的 MLU Stream 上，与主 Stream 中的 Router/Routed Experts 并行执行，在最终结果合并前进行 Stream 同步，从而缩短 MoE 层的整体执行时间。实现可以参考 vLLM 的 Shared Expert overlap，以及 SGLang 中其他 MoE 模型已有的 dual-stream 实现。
4. 你现在给出的方案是有类似的模型这样做吗有参考依据吗，为什么可以这样设计，目前仓库中这部分处于什么状态

qwen2的moe都已经实现这种方案了吗，该实际案例在sglang的官方仓库中吗，请查找官方社区，找到实际案例

分析一下这个welm4的代码文件，介绍一下实现了哪些功能。介绍函数或者代码块

所以说我理解的当前仓库已经沿用的是qwen2moe的实现框架，相关并行或者说共享专家的stream已经定义好了只是没有实现所以目前还是串行的状态，后续只需要按照qwen2moe的案例补齐相关代码就可以是这样吗

所以说我理解的hook是本来沿用qwen2moe的代码文件，其中关键的一些函数或者算子用mlu自己的代码，但整体框架仍然是上游的版本

首先查看官方社区qwen2的moe都已经实现这种并行方案了吗，该实际案例在sglang的官方仓库中吗；其次如果官方实现的话是不是套用官方案例和实现方法即可

目前状况：当前 WeLM MLU 已经创建并把 `alt_stream` 传到了每个 MoE Block，但 `Qwen2MoeSparseMoeBlock.forward()` 仍按 `Shared Expert → Router → TopK → Routed Experts` 顺序串行执行；现在需要参考 SGLang upstream Qwen2-MoE 的 dual-stream 实现，把现有 `alt_stream` 真正用于 Shared/Routed overlap，并同时处理普通路径和 `router_context/gather_expert_hidden` 路径以及最终同步。

写一份实现该并行策略的方案，内容包括：1-目前welm仓库路由专家和共享专家是怎么样的，它的执行逻辑和具体代码函数等等；2-为什么选择qwen2moe的并行执行方案，给出选择的实际依据比如官方的实现在什么代码什么位置等等；3-给出目前welm仓库的实现方案，具体到应该修改哪些函数或者代码，逻辑怎么样

联网搜索官方社区qwen2moe的并行实现方案是不是这样，给出可靠的实现依据

- 整理obsidian,学习使用方法，是否可以通过github在不同地方产生链接
介绍一下moe模型中的mlp,从隐藏状态进入到moe的mlp输出，经过了哪些结构，举一个实际例子说明

刚才说的gather_expert_hidden=True,意思是隐藏状态经过topk之后又重新整理隐藏状态的形状吗，四卡下的true和false分别怎么理解

--tp-size 4--ep-size 4，在这个配置下进入moe时各卡上的情况是怎么样的，在moe上是tp=1吗，也就是专家是分散在四张卡上，但是每个专家的权重在卡上都是完整的，共享专家则是每个卡上都是完整的，那各卡上的隐藏状态都是完整的吗还是切分的，举一个实际的例子说明过程

moe-a2a-backend分别指定none和deepep下是什么情况，--tp-size 4--ep-size 4只能是none吗，这个参数是否决定了进入moe时各卡的隐藏状态是完整的还是被切分的

router_context is None或gather_expert_hidden == False到底怎么理解这两个参数，前一个参数是根据多卡的配置参数系统自行选择吗，后一个参数是根据第一个参数决定的吗，这两个参数决定的是各卡能不能拿到完整的隐藏状态吗

- --tp-size 4--ep-size 4，这种情况下，模型是如何切分的，在进入moe之前各卡上的是完整的token数吗
- deepep和alltoall的学习，moe部分的融合算子以及何时通信

在该仓库下启动参数为--tp-size 4--ep-size 4--moe-a2a-backend none这种情况下，router_context和gather_expert_hidden这两个参数值是怎么样的，这两个参数是由什么决定的
；模型是如何切分的，在进入moe之前各卡上的是完整的token数吗

- Prefill Context Parallel 特殊路径 以及 Token Owner 路径是什么（在学习cp之前先复习tp和ep）
router_context.local_rows(...)和router_context.prepare_moe_inputs(...)是什么

进入容器mahao_welm_all,切换到文件夹/workspace/sglang-mlu进行修改代码（尽量保持代码简洁，读取仓库agent修改代码规范）：参考 SGLang Qwen2-MoE dual-stream，实现 Shared Expert+Gate 放 `alt_stream`，Router→TopK→Routed 保持主 Stream，兼容 `router_context/gather_expert_hidden`，Add 前用 `wait_stream` 同步，仅支持 `moe-a2a-backend=none`

不要修改当前backup/sglang-mlu的工作区代码，修改的是进入指定的容器，修改容器中的文件

将这次修改整理成一个修改报告，生成md文件，保存在宿主机的docs文件夹内

--welm-shared-embedding-policy disabled--enable-welm-kv-mirror-opt--disable-hybrid-swa-memory--trust-remote-code这几个启动参数是什么意思

要验证代码修改后是否双线并行，我需要使用cnperf来抓profile查看，这两个代码文件需要修改什么内容


- 学习一下showme的skill使用

%% 项目关联导航：开始 %%
## 项目关联导航

- 所属模块：[[outputs/项目整理/模块-实习日记|模块-实习日记]]

%% 项目关联导航：结束 %%
