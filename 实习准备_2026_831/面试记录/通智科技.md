# 2026-08-17 面试录音整理稿

> **录音时间**：2026年8月17日 下午 2:40  
> **录音时长**：30分09秒  
> **整理说明**：在不删除、不补充、不改变原始问答含义的前提下，对录音转写进行排版规范化。保留全部追问、数字、回答不清楚或记不清等细节；仅纠正上下文可以明确判断的技术术语与语音识别错误。无法确定的词语保持原转写。

## 关键词

`矩阵` `压缩` `文本` `架构` `长度` `线程` `注意力` `推理` `解码` `结构` `上下文` `主模型` `语法` `空间` `缓存` `位置编码` `位置信息`

## 主题索引

| 时间段 | 主题 |
|---|---|
| 00:00–01:48 | Qwen3.5 架构、GDN 状态与 KV Cache |
| 01:49–04:11 | 抢占重算与 CUDA Graph |
| 04:12–05:52 | Qwen3.5 多模态输入 |
| 05:53–09:56 | MTP、Draft Model 与接受率 |
| 09:57–14:08 | KV Cache 压缩与逻辑/物理长度 |
| 14:09–17:25 | TP=4、Attention/FFN 切分与通信 |
| 17:26–20:03 | GEMM Kernel 优化 |
| 20:04–23:54 | FlashAttention、Continuous Batching、量化与投机解码 |
| 23:55–25:28 | Chunked Prefill |
| 25:29–26:41 | C++ / CUDA 与 Python 调用 |
| 26:42–27:55 | DeepSeek、近期工作与手写 GEMM |
| 27:56–29:55 | 候选人反问与岗位方向 |

## 完整文字记录

### 1. Qwen3.5 架构、GDN 状态与 KV Cache（00:00–01:48）

**候选人｜00:00**  
是这意思吧？

**面试官｜00:02**  
你给我介绍一下 3.5 的架构跟 3 的架构有什么区别？

**候选人｜00:08**  
3.5 的架构它最主要的区别就是在它的模型结构层上，它的每个 decoder 层它都是是一个 GDN 层和和 attention 层的一个混合。它的 GDN 层的话，它主要主要保存的它就不是一个cache，它保存的是两个的一个结构，固定长度的结构。那个注意力层它保存的是主要是cache，它是会随着请求的那个 token 的增加，它那个 cache 是会增长的。这是主要的模型结构，其中它还有一些细小的改变，比如说Qwen3.5 的话，它RoPE，那个编码就是位置编码，它使用的是MRoPE，就是一个 3D 的嘛，因为它有多模态。别的可能没有大的更改吧。

**面试官｜00:55**  
好的。那它在架构改了之后，它的 KV Cache存储的时候有对应的改变吗？

**候选人｜01:03**  
KV Cache的存储的位置就是它之前的方法还是没有改变，只不过在适配新模型的时候，主要做的就是对新增的 GDN 层的两个状态，它又要新增一个给它怎么样调度或者怎么样存储。只是做新增的这个功能，将两个功能互相适配一下。

**面试官｜01:24**  
这两个层，比较特殊这两个层的需要存 KV Cache吗？

**候选人｜01:29**  
新增加的 GD 层，它是不需要存 KV Cache的，它这个有增加这个层的目的，为了解决那个 KV Cache，它是会随着那个 token 的增加而增长，所以说新增的这个层它主要就是它的长度是固定的，它是根据前一个前面的历史信息，来不断的进行递归生成。对。

### 2. 抢占重算与 CUDA Graph（01:49–04:11）

**面试官｜01:49**  
OK，好，那我了解了。然后适配抢占重算与 CUDA Graph，你这块是怎么做的？

**候选人｜02:02**  
主要就是，就我先讲一下，就是主要在原本的千川的结构，它主要是注意力层嘛，我们只需要关注cache，比如说新增在Decode的阶段或者 prefill 阶段，它那个调度器来将新的请求来调度进来的话，它需要判断这个请求，新的新进来请求能不能给它分配那个 cache 的缓存，能不能。

**候选人｜02:22**  
能不能分为 cache block？如果不能的话，我们只需要判断，如果可以的话，我们只需要把它请求放进来就可以，只需要判断那个 cache block 够不够。新增加新增加这个混合层的话，我们就需要同时判断两个条件，第一个条件就是说 cache block cache block 它的空间够不够？要么就是说它的 GDN 层，我们新分配那两个，一个是卷积状态，一个是递归的循环状态，这两个状态 GDN 层它对应的这两个状态，它的显存空间够不够？如果两个都够，才能把这个请求给它放进来。就是做了这样一个改变。

**面试官｜02:56**  
OK，然后你 CUDA Graph 相当于是重新重新录了一遍，是这个意思吗？

**候选人｜03:04**  
对的，之前的话可能就是只是只是在那个 block 结构，它就是Decode的阶段，我可能进行一次就是Decode的阶段，每生成不同的请求进来，比如说有两条请求、四条请求、八条请求，我给它分别给它录那个 CUDA Graph，然后来进行launch，现在现在的话可能重新再做一遍，就这个可能就是比较简单的实现一下。

**面试官｜03:30**  
你了解 CUDA Graph 它为什么要用吗？

**候选人｜03:35**  
因为在本来如果不不启动 CUDA Graph 的话，就是它的每个 CPU 去执行去给 GPU 发送执行的话，一些算子或者一些空间它都会速度比较慢，就每一个请求它都会会要需要执行一次。使用 CUDA Graph 的话，就是按照预定的一个执行，提前执行好的录好的路线，然后去执行它就可以了。

**面试官｜04:01**  
那它具体节省了哪段的时间？你你有了解过吗？

**候选人｜04:08**  
这个说实话，时间长了有点忘了。

### 3. Qwen3.5 多模态输入（04:12–05:52）

**面试官｜04:12**  
嗯，好。然后我再往下看一下啊，你这个Qwen2.5，Qwen3.5 的支持多模态输入，这个是怎么做的？

**候选人｜04:24**  
一般的话，如果是纯文字的话，我们只需要就是给它处理token，然后变成tokenizer，变成tokenID，然后直接输入模型就可以了。千问，多模态的话，主要它这个执行结构是这样的，如果有图片和文本，它都是混合在一块进来的话，它会首先进分两条路线，一条是纯文本的路线，就是按照原本那一套，然后tokenizer，然后变成token，tokenID，然后送进模型。文本的话，它就把图片。他就是把图片先把图片预处理，处理成他他他会先把图片进行预处理，输出两个，两个比较重要的参数，就把图片的一个是图片的信息，一个是图片的位置信息。然后文本的部分啊，他会预留多模态的图片所要预留放的位置，根据根据图片的它的宽或者高，算出算出需要填充到文本多少信息，将多模态的。将图片的信息给它填充到文本的信息当中，再一起进行处理，就转换成统一的一个隐藏状态，再输入模型当中。

**面试官｜05:28**  
OK，这Qwen3.5 是一个原生多模态模型，是吗？它不需要杠 VL 额外的去加VIT，就是 VIT 的部分，它是本身自己就支持这个视觉编码的，是吗？

**候选人｜05:48**  
对的对的。

### 4. MTP、Draft Model 与接受率（05:53–09:56）

**面试官｜05:53**  
然后视频课程注入，然后进行了你这个 MTP 怎么做的？

**候选人｜06:03**  
MTP 的话就是原本的原本的 Token 输出，原本的Decode的阶段生成一个Token，我们就直接是用主模型来输出。 MTP 的话现在每个请求新生成一个 Token 之后，将它的最后生成的一个Token，还有它之前的。前一个 token 的隐藏层一起输入到这个 MTP 这个结构里面，让让 draft model 来给它生成后面的几个token，生成几个 token 之后，如果再一次性发送那个主模型当中去进行验证一下，如果前面几个token。

**面试官｜06:36**  
我知道了 MTP 它本身怎么做，我的意思是Qwen3.5 它是本身有原生的 MTP 的 draft model 吗？还是说是是你训了一个 draft model？

**候选人｜06:47**  
这个 draft model 也不是我训的，只是我就是把那个原本Qwen3.5 的那个注意力层，对，注意力注意力层拿了一层放在旁边。

**面试官｜07:00**  
注意力层拿了一层。哦，你就直接拿的Qwen3.5 的某一层的，没有训，然后直接作为这个预测模型去做的。

**候选人｜07:08**  
对，对，是这样。

**面试官｜07:11**  
OK，那你的接收啊，接收的深度，还有次数，这些就是接收率啊，这些是最后的效果。如果你随便拿一层的话，那你的是，我记得做这个 MTP 的时候，要设置，就是包括你接收，接收的接收长度，还有一些接收的，就是一些。参数你是怎么调配的？怎么设置的？最后它效果怎么样？

**候选人｜07:41**  
参数的话就是我给它设置了，每生成 5 个token，再一次性放进那个主模型里面进行验证。我只是拿取了 Decoder 里面的一层，一层的Decoder，放在旁边进行。

**面试官｜07:56**  
生成 5 个，它这个 5 个，它，因为我之前做图的解码的时候，它这个 5 个不应该是一个 train 的结构嘛？第一轮的预测预测 5 个，比如说第一轮预测预测 10 个，取最大接受率的 5 个，第二轮这 5 个再变成，再往下分，预测 5 个，再。去。接受率最高的，这 25 个里边接受率最高的 5 个，你是怎么做的他的预测的接收的这块，你是怎么做的？到底具体。

**候选人｜08:28**  
接收的。

**面试官｜08:29**  
你明白我什么意思吗？

**候选人｜08:30**  
我大概明白吧，接收的这边我就是用了一个贪婪采样，就是很简单的只看他概率最高的那一个，只取他概率最高的那一个。

**面试官｜08:38**  
每次只拿一个，每次只拿一个。

**候选人｜08:40**  
对，因为我只主要是只做了一个简单的那个MD。

**面试官｜08:45**  
那你那你那个验证的时候，实际上就只拿了一段连续的序列去做了验证，就没有没有分。没有多层级，多个组的验证，是这意思吗？

**候选人｜08:59**  
就是它每一个 forward 它只生成一个token，然后我就等它生成连续 5 个 token 之后，把这 5 个放进主模型里面进行。

**面试官｜09:10**  
那你这样的话，你的接受率高吗？

**候选人｜09:12**  
接受率测了一下，大概 60% 吧。

**面试官｜09:17**  
60% 是 5 个里边收 3 个的意思吗？你指的是？

**候选人｜09:20**  
对，是这样的。

**面试官｜09:23**  
这么少吗？我自己训的模型，而且我的。我自己训的模型，我还是树结构的验证的方法的情况下，每轮接收 5 个，深度为 30 的情况下，我的接收率只有二点几，我不清楚为什么，你的接收长度和接收率，接收长度就相当于有 3 左右。

**候选人｜09:54**  
对，差不多吧。

### 5. KV Cache 压缩与逻辑/物理长度（09:57–14:08）

**面试官｜09:57**  
好，我了解了。然后你再给我讲一下你这个 KV cache 压缩是怎么做的？

**候选人｜10:03**  
KV cache 压缩就是在原本的 kv 的基础上，先让它比如说我要，它是一个周期性的，滑动窗口式的压缩。比如说我先生成1024，我这个先默认值是1000，生成 1024 个token，生成 1024 个 token 之后，它生成相对应的， KV cache，我检测值就是到达 1024 之后，我进行开始压缩，就是在这 1024 个生成的 KV cache 当中，选取它的最重要的 512 个 KV cache，挑选出来，剩下的给它去掉，把 512 个 KV cache 再给它压紧。

**面试官｜10:42**  
你这个怎么筛选重要不重要？

**候选人｜10:46**  
就是使用了一个 SnapKV 思想的一种方法，就是用一第，比如说我一生成 1024 个token，我用第第 1024 个 token 的q，然后去查询之前历史历史的开和v，历史的开，然后做注意力计算，然后得到重要性分数，然后选取最高的 512 个。

**面试官｜11:06**  
然后再把这 512 个进行什么？进行一个压缩。

**候选人｜11:10**  
对，进行一个压缩，然后剩下的 512 个就给它直接释放掉。

**面试官｜11:16**  
有个问题，你这 512 个压缩怎么做的？

**候选人｜11:21**  
就是选取，它每个 block 每个block，每个 token 它都有自己的位置，用那个调度器，调度它，就是把相应的那些需要保留下来的位置给它保留下来，不需要的位置就给它直接用，给它释放掉，用 BlockManager 给它释放掉。

**面试官｜11:41**  
不是，你听我的问题啊，比如说 1024 个，你是 512 个，筛 512 个出来吗？根据 Q 和 KV 的计算，最后一个 token 的 Q 和前面的 KV 的计算，筛出来 512 个了吗？

**候选人｜11:55**  
对，筛出来。

**面试官｜11:56**  
你筛出来这 512 个还要再压缩吗？还是说就把这 512 个保留，把剩下的释放掉？

**候选人｜12:01**  
对，就把这 512 给它保留掉，剩剩下就给它释放掉。

**面试官｜12:05**  
那你这 512 个保留的情况下，你的 KV cache 是不是要重新在一个，你的 KV 的 block 是不是要重新再分配了？如果你要释放掉其他的话。

**候选人｜12:18**  
对，之前的，之前的因为剩下的一个 block 里面有，一个 block 比如说是 512 个，然后 1024 它有两个block，两个 block 的话，把之前的那些空间那些token，就直接给它释放掉，释放的这一轮就先不继续forward，继续把这一轮的前面，把后面的 token 全部放到前面，给它进行压缩到前面去。

**面试官｜12:47**  
好，我明白你什么意思了。那你这样的话，你的位置编码是不是就不连续了呀？

**候选人｜12:55**  
位置编码。

**面试官｜12:57**  
对，你的 KV 的位置编码是不是就不连续了？

**候选人｜13:01**  
再给，对，它好像是不连续了。

**面试官｜13:07**  
那位置编码不连续的情况下，你相当于你，你那个在 VLM 里边的每一个 sequence 的max，当前的长度，这些都要对应的改变吗？

**候选人｜13:24**  
对，它这些长度，我之前用它的最大长度进行一个复制，之前用一个参数统计这些，现在变成两个参数，之前的原先的参数还是保留之前原先的长度，现在新的参数就是把它压紧之后的长。参数是多少，后面就只有这个新的参数。

**面试官｜13:42**  
一个真实，一个原本，是这意思吧？

**候选人｜13:44**  
对，给他复制一个，复制那个相关的参数。

**面试官｜13:47**  
哦，关键 KV Cache减少并发请求。OK，明白你这是做的什么了。然后你给我讲一下这个 TP 等于 4 的这个，TP、 TP 的时候，这个 QKV 是怎么那个什么，怎么切的？你明白我什么意思吗？

### 6. TP=4、Attention/FFN 切分与通信（14:09–17:25）

**候选人｜14:09**  
明白，然后在 TP 等于 4 的时候，就是。我比如说 QK 在算 QK 的时候，它是按行切，不是，它是按列按列切到四个卡上面。最后再进行 QKV 计算之后，进输入一个，它的注意力计算完成之后，输入一个 O 矩阵， O 矩阵的话，它就是在进行一个按行的一个输入，最后 allreduce 上， allreduce 到一个卡上面，后面做计算。

**面试官｜14:41**  
你刚说的是QK，是吧？

**候选人｜14:42**  
对。

**面试官｜14:44**  
V 和 O 这。

**候选人｜14:47**  
就是， QK 的话，它就是按列按列，就是在每个卡上各自算各自的，输出的 O 矩阵，就是它算完之后的一个隐藏层，它它再聚集到一个卡上面，就 all reduce，一个卡上面，做后面做后面的处理。

**面试官｜15:03**  
那 V 矩阵怎么切呢？

**候选人｜15:06**  
V 矩阵，这个好像也也了解有点少。

**面试官｜15:11**  
就是你，你计算的时候， Q 和 K 都是按列切吗？

**候选人｜15:19**  
不是，

**面试官｜15:23**  
哦，因为是要要转置，是吧？对。然后 Softmax 之后，QK，Softmax， FFN 的这些切，你有了解吗？

**候选人｜15:40**  
FFN 它切也是跟 QK 也是类似的，它先进行一个，先先将，先进行升维，就是 UP 先进行升维，然后它那个门控信号，门控的那个值也，门控那个矩阵也是按列切的，就是它跟 QK 是一样的，最后进行进行降维的时候。 down projection 降维的时候也是进行一个 all reduce，到一个卡上面。

**面试官｜16:03**  
怎么我听你讲的全是 all reduce？我怎么记得还有一个是？gather。一个是 all reduce，还有一个是什么？一个是。因为我当时。

**候选人｜16:12**  
就是那个词表，它最后不是有一个词表，将就是，就它最后生成已经生成 token id 了嘛，它要把这个 token id 转换成一个文本语言的时候，它就在四张卡上面，然后进行一个gather，聚集到一张卡上面，进行一个将各个卡上的那个矩阵进行相加。加到最后一个卡上面，这个应该是 gather 吧。

**面试官｜16:36**  
OK，你这个 KV Cache压缩这块，你的精度有下降多少吗？

**候选人｜16:43**  
这个我没有统计精度，就是精度它肯定是有损的，因为它在长文本的情况下，我我只测的时候，测了就是它的block，它到底，就显存它的。

**面试官｜16:56**  
block 降47%，正常。对。然后零抢占最大并发4，提升至8， decoder 吞吐，你的 decoder 吞吐为什么会上升啊？

**候选人｜17:09**  
因为它可能是长文本的那个 KV 变少了。

**面试官｜17:12**  
是吧？对。是这意思吧？对。OK，接收率到63%。

### 7. GEMM Kernel 优化（17:26–20:03）

**面试官｜17:26**  
GEMM Kernel 的实现，你给我讲一下这个 Kernel 你优化的过程吧。

**候选人｜17:34**  
Kernel 就是首先它是在最原始的状态，它就是进行两个矩阵相乘的时候，就是它每一行和每一列相乘，它就是。从直接从显存当中去把 AB 两个矩阵，要计算哪一个数，直接拉到 GPU 里面去进行，每个线程它就计算一次，所以说是比较浪费空间的。后续的话就是先将 AB 矩阵，它先进行一个分阶段的一个一个切割。比如说先将一个矩阵它切到多少个线程块，每个线程块再切多少个Warp，每个 Warp 剩下多少个，每个 Warp 的话它可能是 32 个线程，这样分下来，每个线程它就不止计算一次。

**候选人｜18:16**  
矩阵乘，不止计算一次里面的乘法，这个是按线程块分，后续的话再就是用了一个异步的缓存，异步的读取数据，将先将 IB 的数据先从显存给它读取到共那个共享，共享内存的共享空间当中，再进行计算，就放到 GPU 计算，这样速度就会快一点。双缓，用了一个双缓存的双缓双缓存的机制吧，边进行计算的时候，边从显存空间中把数据读取到共享空间之内。后续再就是做了一个矩阵的，各个空，不同不同尺寸矩阵的一个，分分布走的一个流程，比如说一个，我就是。每次，比如说他就， A 矩阵是 M×K，然后 B 矩阵是 K×N，他 K 的话，我每次只取他的一小部分，每个一小部分，如果他是正常运算的话，我就让他走标准的运，直接，比如说 K 等于8，我先取 8 个那个数据过来直接运算，如果他最后做几行或者几列，他不足 8 的话，再给他用另外一个另外一个计算方法。

**面试官｜19:27**  
这个在向量优化的时候，有哪些需要解决的问题点？你能比如说会存在什么比较常见的一些问题点，你有了解过吗？

**候选人｜19:40**  
这个了解不太多。

**面试官｜19:42**  
就比如说什么你有对一个算子进行一个 prefill fill 的看它到底的瓶颈在哪里？这些有实现过吗？还是说就是写了一些这些算子？

**候选人｜19:57**  
简单写了一些这些算子吧，我对这个算子确实，还没学到很多，主要是做了一些推理优化的。

### 8. FlashAttention、Continuous Batching、量化与投机解码（20:04–23:54）

**面试官｜20:04**  
OK，那聊一聊推理优化这边的话，你对 FlashAttention 了解吗？

**候选人｜20:12**  
这个只了解到概念层次吧，它就 FlashAttention 它就是对 attention 计算的这块，做了一些优化，之前。

**面试官｜20:24**  
他具体怎么做的呢？

**候选人｜20:26**  
这个很久之前看过，现在可能有点忘了。

**面试官｜20:30**  
他具体优化了哪些内容呢？你给我讲一下他优化的，解决的问题点在哪里？

**候选人｜20:39**  
这个确实记不太清了。

**面试官｜20:43**  
OK。那你在做 vLLM 的时候， vLLM 的特性嘛，它是有 kv cache 嘛。然后你知道 sg 浪吗？

**候选人｜20:58**  
我我只做过 vLLM 相关的一些。

**面试官｜21:01**  
OK， vLLM 这块儿它还有什么东西？那你给我讲一下这个 Continuous Batching 这块儿你了解吗？

**候选人｜21:10**  
这个有了解。

**面试官｜21:12**  
你给我讲一下。

**候选人｜21:13**  
就是我这个项目它也用到了那个 Continuous Batching，如果是静态的班审的话，它比如说有八个请求进来，它必须要等最长的请求给它运算完之后，才算一次包络的整个流程结束。一批半时，里面如果有 8 个请求，它有一个如果是很长的请求的话，那些短请求你早，你本来可以用更短的时间，但是你现在必须要等长请求，包络的结束之后，你一批的。动态班审的话，你短请求结束之后，你立马加新的请求进来，然后跟在它的后面，继续做。

**面试官｜21:45**  
怎么做到的？

**候选人｜21:51**  
我想一下。

**面试官｜21:53**  
嗯，他是怎么做到的？他为什么可以不用等了？

**候选人｜22:07**  
这个没有了解的。

**面试官｜22:19**  
你对量化压缩有了解吗？

**候选人｜22:22**  
量化压缩了解的不多。

**面试官｜22:35**  
我这。解码，你了解哪些投机解码的什么架构？

**候选人｜22:44**  
投机解码就是之前的有 EAGLE 架构，它有好几个版本，它主要解决的就是，就是它主要用的方法，我就是参考的它这个 EAGLE 的方法，做的我这个MTP，它就是并行运算，后面我大概了解过 deflash 和despark， deflash 的话它就是主要解决 token 一次生成，生成并行并行生成token，那个 draft model 一次生成一批的model，不是一批一批的token。 DSpark 的话，它是在这个 DFlash 的基础上，让后面的 Token 之间，它会再加一个顺序的结构，让后面的 Token 之间产生联系，可能让后面的 Token 生生成的准确率高一点，我只了解到这个层次。

**面试官｜23:36**  
你给我讲一下 Ego 123，它们的进阶变化有哪些？你能了解出来吗？

**候选人｜23:44**  
这个就有点忘记了。

**面试官｜23:46**  
那 EAGLE-3 你熟吗？ EAGLE-3。

**候选人｜23:50**  
这些不是很熟，就是只知道它是并行的生成。

### 9. Chunked Prefill（23:55–25:28）

**面试官｜23:55**  
Chunked Prefill，我看你这儿也写了，你给我讲一下。

**候选人｜24:01**  
我没听清楚，不好意思。

**面试官｜24:03**  
那个 Trunk 这个 Chunked Prefill。

**候选人｜24:06**  
Chunked Prefill 就是一次 prefill 如果长请求的话，它是它的那个token， prompt 数是比较长的， trunk prefill 就是把它切成比较短的短的。Prefill，做好几次，输出，对，是这样的。

**面试官｜24:23**  
他为什么要做窗口？

**候选人｜24:26**  
因为一次长，一如果即使输入输入一批的 Prefill 的话，他可能长请求占用时间太多，或者资源的压力， GPU 计算压力很大，给他切成。

**面试官｜24:46**  
这块不应该是做了，因为长请求，长 Prefill 进来的话会阻塞 decoder 吗？

**面试官｜24:58**  
我怎么记得是，比如说不同长度的 prefill 进来，比如说短的 prefill 结束了， decoder 一直会被 prefill 阻塞。

**候选人｜25:10**  
对啊，对，是这样的。

**面试官｜25:13**  
所以它会有一个 trunk 的，里边会保留几个 decoder 的位置。

**候选人｜25:20**  
对，它如果是长期的话，它 TTFT 可能就时间比较长。

### 10. C++ / CUDA 与 Python 调用（25:29–26:41）

**面试官｜25:29**  
你平时写 C++ 多吗？

**候选人｜25:32**  
C++ 写的不多。

**面试官｜25:36**  
C++ 你有了解一些语法方面，问你个 C++ 的语法的话，需继承，虚继承，这些你有了解吗？

**候选人｜25:46**  
这些之前，这个时间已经比较长了，之前做过，最近可能没有写很多。

**面试官｜25:53**  
那这样，我问你啊，你 C++ 写库大的时候，你怎么让 Python 调的？

**候选人｜25:59**  
那写 CUDA 的时候。

**面试官｜26:01**  
你 C++写的 CUDA 吗？对吧？怎么能让 Python 去调出来？

**候选人｜26:06**  
这个我确。Python。我之前就是写 Cuda Cuda 文件，就是给它调， import 一些东西，给它调进来。

**面试官｜26:18**  
什么什么？我没听清。

**候选人｜26:20**  
就是写 Cuda 的时候，就 Cuda 文件写好了，这个当时因为是那个 Cuda Cuda 的语法不太熟悉嘛，其实用AI，用 AI 让它直接直接给我把 Cuda 文件，用 Python 给它调出来，过程当时只了解了一些技术细节，那些语法的啥的我就没怎么细看了。

### 11. DeepSeek、近期工作与手写 GEMM（26:42–27:55）

**面试官｜26:42**  
哦， DeepSeek 的主流架构 V4 这一版你有了解吗？

**候选人｜26:53**  
这个没了解。

**面试官｜26:55**  
我看你这上面写的呀，熟悉千问、 DeepSeek 等主流架构模型啊。

**候选人｜27:00**  
这个简，这个简历好久，前几个月之前做的，现在复习没复习到这个上面。

**面试官｜27:06**  
OK，那你最近在做什么？你跟我聊聊。

**候选人｜27:09**  
我最近就是主要在做第一个项目，就是那个推理项目。相关的。

**面试官｜27:14**  
book。

**面试官｜27:22**  
你给我写，算了，你。给我写一个啥吧，写个 GEMM 吧，最简单的，单线程单数字的。

**候选人｜27:33**  
这个写的不多，可能写不出来。

**面试官｜27:36**  
写不出来是吗？

**候选人｜27:37**  
对。

**面试官｜27:40**  
好吧，OK，我我这边没有什么问题了。好，你有什么想问的吗？

### 12. 候选人反问与岗位方向（27:56–29:55）

**候选人｜27:56**  
我想问一下，如果实习生进去的话，参与一些什么公司的项目，或者说公司内部正在做什么东西？

**面试官｜28:05**  
他目前正在做的是，他现在在做的是给 SGLang社区提一些 PR 或者什么的一些东西。就他具体做什么，这个 PR 内部具体是什么就不说，就是说他现在在给 SGLang或者一些开源社区做一些那什么，他需要后面要做的东西是一个。 VLM 和他想做一个模型自进化的推理训练并行框架。你做 VLM 嘛，你知道在做推理的时候会生成大量的 KV cache 嘛，我看你这儿也有做 KV cache 压缩的。然后其实这些所有的你这个上下文还是比较短的，有一些比较特别特别长的上下文，他们现在不是用 RAG 或者是用的其他的技术在做的这种搜索，或者是什么嘛，他希望做的是把他们的对话，还有他们的上下文。

**面试官｜29:06**  
还有所需要的一些知识，从你所关注的一些信息，从网上重新去下载，重新构成一个数据集，进行训练，你的推理和训练的引擎变成一个共有的引擎，把。把你所生成的上下文和所需要的知识呢，训练成一个额外的一个LoRA，外挂在这个模型外边。行。然后相当于进行一个模型的资金化，然后每过一定的周期，上下文满了之后呢，进行一个资金化，然后但是并不会阻塞推理的需求，之后呢，训练好的 LoRA 的模型放在那个什么上面，大概就是做这样一个具体的内容。

**候选人｜29:45**  
行，好的，好的，我这边也没有要了解。

**面试官｜29:50**  
OK，好，那我今天面试就到这里。

**候选人｜29:53**  
嗯，好的，好的，谢谢面试官。

**面试官｜29:55**  
OK。


%% 项目关联导航：开始 %%
## 项目关联导航

- 所属模块：[[outputs/项目整理/模块-面试记录|模块-面试记录]]

%% 项目关联导航：结束 %%
